{:paths ["src" "test"]
 :tasks {test {:doc "Run the cljc contract + behavioral test under babashka"
               :extra-paths ["test"]
               :requires ([tesla_api.main-test])
               :task (let [{:keys [fail error]} (clojure.test/run-tests 'tesla_api.main-test)]
                       (System/exit (if (pos? (+ fail error)) 1 0)))}}}
